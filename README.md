## Estudiantes

Marco Álvarez, 
Yurgen Cambronero, 
Manfred Jones

## Compilación y Ejecución

En la carpeta principal, es decir, en miCompilador, ejecutar el siguiente comando:

- `compilar y ejecutar ant`: ant run -Dapplication.args="test.txt out.txt"


> Cambia el nombre del ej.txt por el nombre del archivo con el código a probar, recuerda que este archivo debe encontrarse en la misma ruta que el proyecto, o más sencillo, en la misma ruta donde esta el ej.txt!

> Para ejecutar este proyecto es necesario tener descargado ant y haber puesto la ubicación de la carpeta "bin" en la variable "Path" de las Variables de Entorno del sistema.